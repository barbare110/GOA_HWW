# Slicing allows you to extract a part of a list using the syntax list[start:end], where:
# - 'start' is the index where the slice starts (inclusive),
# - 'end' is the index where the slice ends (exclusive),
# - Omitting 'start' or 'end' means using the start or end of the list respectively.